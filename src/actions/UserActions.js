import { createAction } from 'redux-actions';

export const changeSearch = createAction('CHANGE_SEARCH');
export const changeViewBy = createAction('CHANGE_VIEW_BY');
export const changeGraph = createAction('CHANGE_GRAPH');
